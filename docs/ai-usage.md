# AI Usage Log

This document records verified AI-agentic tool usage only. It claims application implementation and runtime testing only where those actions are directly recorded below. It does not claim deployment, hosted repository configuration, final submission, or video generation.

## Verified So Far

| Date | Tool | Purpose | Files affected | Commands run | Outcome | Limitations or corrections |
|---|---|---|---|---|---|---|
| 2026-07-22 | Codex | Requirements traceability and repository governance preparation | `AGENTS.md`, `docs/governance.md`, `docs/workspace.md`, `User_story_Roadmap.md`, `.gitignore` | `pwd`, `find`, `sed`, `wc`, `rg`, `git init`, `git branch -M main`, `mkdir`, `git status --short`, `git diff --check` | Governance and workspace documentation prepared before application code | Batch 1 over-narrowed roadmap source wording; later repaired to use `Assignment.md` as complete source authority |
| 2026-07-22 | Codex | Architecture documentation and TSD drafting | `docs/TSD.md`, `docs/architecture.md`, `docs/testing.md`, `docs/decisions/ADR-001-pgvector.md` | `sed`, `rg`, `git status --short`, `find`, `git diff --check` | Pre-implementation design documented | No runtime tests or implementation performed |
| 2026-07-22 | Codex | Feature-specification drafting, documentation consistency review, and roadmap evidence synchronization | `docs/features/post-creation.md`, `docs/features/feed-ranking.md`, `docs/features/semantic-search.md`, `docs/features/interactions.md`, `docs/features/mobile-feed.md`, `User_story_Roadmap.md`, `docs/ai-usage.md`, `docs/TSD.md`, `docs/architecture.md`, `docs/testing.md` | `sed`, `rg`, `git status --short`, `find`, `git diff --check`, `file` | Feature specifications documented and consistency-reviewed before application code | No application implementation, tests, runtime verification, deployment, hosted repository configuration, working application generation, or video generation performed |
| 2026-07-22 | Codex | Backend foundation implementation, database schema implementation, embedding-service implementation, API endpoint implementation, automated-test creation, and documentation synchronization | `api/`, `embedding-service/`, `docker-compose.yml`, `.env.example`, `README.md`, `docs/TSD.md`, `docs/testing.md`, `docs/workspace.md`, `docs/ai-usage.md`, `User_story_Roadmap.md` | `pwd`, `sed`, `find`, `git status --short`, `docker compose build api embedding-service`, `docker run --rm guisedup-api php artisan test --testsuite=Feature`, `docker run --rm guisedup-embedding-service python -m pytest`, `docker compose up -d db`, `docker compose ps`, `docker run --rm --network guisedup_default ... php artisan migrate --force`, `docker compose exec db psql ...`, `docker run --rm guisedup-api php artisan route:list`, `git diff --check`, `rg` | Laravel foundation, PostgreSQL/pgvector migrations, Sanctum-protected post and interaction endpoints, Python fallback embedding/authenticity service, Docker Compose configuration, README, `.env.example`, and focused tests were created and verified as stated | At the time of this entry, feed ranking, semantic search, mobile, and SQL were not yet implemented; later entries record their completion. Deployment, hosted repository configuration, final submission, and video were not performed; transformer model execution was not verified |
| 2026-07-22 | Codex | Roadmap audit repair and `GET /api/feed` implementation | `User_story_Roadmap.md`, `api/routes/api.php`, `api/config/feed.php`, `api/app/Http/Controllers/FeedController.php`, `api/app/Http/Requests/FeedRequest.php`, `api/app/Services/Feed/*`, `api/app/Providers/AppServiceProvider.php`, `api/tests/Feature/FeedRankingTest.php`, `README.md`, `docs/TSD.md`, `docs/testing.md`, `docs/workspace.md`, `docs/features/feed-ranking.md`, `docs/ai-usage.md` | `sed`, PDF outline extraction with `python3`, `docker compose build api`, `docker run --rm guisedup-api php artisan test --testsuite=Feature`, `docker run --rm guisedup-api php artisan route:list`, `docker run --rm guisedup-embedding-service python -m pytest`, `docker compose config`, `git diff --check`, `rg`, `git status --short` | Roadmap stale claims were corrected; `GET /api/feed` was implemented behind candidate-repository and ranking-service boundaries; Laravel feature tests passed with feed-ranking coverage | At the time of this entry, search, mobile, and SQL were not yet implemented; later entries record their completion. Deployment, hosted repository configuration, final submission, and video were not performed |
| 2026-07-22 | Codex | `GET /api/search` implementation, focused search tests, pgvector verification, and documentation synchronization | `api/routes/api.php`, `api/config/search.php`, `api/app/Http/Controllers/SearchController.php`, `api/app/Http/Requests/SearchRequest.php`, `api/app/Http/Resources/SearchResultResource.php`, `api/app/Services/Search/*`, `api/app/Providers/AppServiceProvider.php`, `api/tests/Feature/SearchTest.php`, `README.md`, `docs/TSD.md`, `docs/testing.md`, `docs/workspace.md`, `docs/features/semantic-search.md`, `docs/ai-usage.md`, `User_story_Roadmap.md` | `sed`, `find`, `git status --short`, `docker compose build api embedding-service`, `docker compose build api`, `docker run --rm guisedup-api php artisan test --testsuite=Feature`, `docker run --rm guisedup-embedding-service python -m pytest`, `docker run --rm guisedup-api php artisan route:list`, `docker compose config`, `docker compose ps`, `DB_PORT_FORWARD=55432 docker compose up -d db`, `docker run --rm --network guisedup_default ... php artisan migrate:status`, `docker compose exec db psql ...`, `git diff --check`, `rg` | `GET /api/search` was implemented behind search orchestration, candidate-repository, similarity-calculation, temporal-intent, and response-resource boundaries; Laravel feature tests passed with search coverage; Python tests still passed; Docker builds and Compose config passed; PostgreSQL pgvector operator execution on `posts.embedding` was verified in a rolled-back transaction | At the time of this entry, mobile and SQL were not yet implemented; later entries record their completion. Deployment, hosted repository configuration, final submission, video, and actual transformer model execution were not performed |
| 2026-07-22 | Codex | SQL challenge implementation and PostgreSQL verification | `sql/queries.sql`, `README.md`, `docs/testing.md`, `docs/ai-usage.md`, `User_story_Roadmap.md` | `sed`, `find`, `git status --short`, `docker compose ps`, `docker run --rm --network guisedup_default ... php artisan migrate:status`, `docker compose exec db psql \\d ...`, `docker compose exec -T db psql ...`, `git diff --check`, `rg` | D1 through D4 raw PostgreSQL queries were created, executed against representative rolled-back fixtures, and explained with `EXPLAIN`; rollback cleanup confirmed zero fixture rows remained | No performance benchmark was performed. At the time of this entry, React Native was not yet implemented; later entries record mobile completion. Deployment, hosted repository configuration, final submission, and video were not performed |
| 2026-07-22 | Codex | Expo React Native feed screen implementation and mobile verification | `mobile/`, `.gitignore`, `README.md`, `docs/testing.md`, `docs/ai-usage.md`, `User_story_Roadmap.md` | `sed`, `find`, `git status --short`, `node --version`, `npm --version`, `npm install`, `npm run typecheck`, `npm test`, `npm run start -- --localhost --port 8091`, `git diff --check`, `rg` | One Expo Feed Screen was implemented with separated API client, state hook/reducer, reusable post card, and theme tokens; TypeScript type-check passed; 7 mobile state tests passed in this pass, with later entries recording expanded coverage and Expo Web verification | Simulator/device rendering was not verified in this pass; `npm install` reported 11 moderate vulnerabilities; deployment, hosted repository configuration, final submission, and video were not performed |
| 2026-07-22 | Codex | Final integration audit, blocking-defect fixes, Docker/API/SQL/mobile verification, and documentation synchronization | `.gitignore`, `docker-compose.yml`, `api/public/index.php`, `api/phpunit.xml`, `api/bootstrap/cache/.gitkeep`, `api/storage/framework/cache/.gitkeep`, `api/storage/framework/sessions/.gitkeep`, `api/storage/framework/views/.gitkeep`, `api/storage/logs/.gitkeep`, `mobile/src/hooks/useFeedController.ts`, `mobile/src/state/feedReducer.ts`, `mobile/tests/feedReducer.test.ts`, `README.md`, `docs/TSD.md`, `docs/testing.md`, `docs/ai-usage.md`, `User_story_Roadmap.md` | `sed`, `find`, `rg`, `git status --short`, `docker compose config`, `DB_PORT_FORWARD=55432 docker compose up -d --build`, `docker compose ps`, `docker compose exec api php artisan migrate --force`, `docker compose exec api php artisan db:seed --force`, `docker compose exec db psql ...`, `docker compose exec api php artisan test`, `docker compose exec embedding-service python -m pytest`, `docker compose exec api php artisan route:list`, `curl`, `python3` localhost smoke script, `npm ci`, `npm run typecheck`, `npm test`, `npm run start -- --web --port 8092`, `npm run start -- --localhost --port 8091`, `git diff --check` | Final audit fixed Docker Compose runtime config, tracked required Laravel runtime directories, added Laravel `public/index.php`, isolated PHPUnit test env, fixed mobile stale-search and duplicate-pagination race risks, verified authenticated API smoke behavior, SQL queries, tests, Docker services, and mobile startup | No PDF was read; Expo Web was unavailable without additional web dependencies; simulator/device rendering, private GitHub publishing, deployment, final submission messaging, and video generation were not performed |
| 2026-07-23 | Codex | Clean-checkout submission-readiness verification and blocker repair | `.gitignore`, `README.md`, `docs/TSD.md`, `docs/testing.md`, `docs/ai-usage.md`, `User_story_Roadmap.md`, `api/Dockerfile`, `api/bootstrap/app.php`, `api/composer.lock`, `api/tests/Unit/.gitkeep` | PDF extraction with temporary `pypdf` virtualenv, `git ls-files`, `docker compose build --no-cache`, `docker compose up -d`, `php artisan migrate --force`, `php artisan db:seed --force`, `php artisan test`, `python -m pytest`, `php artisan route:list`, `curl`, authenticated HTTP smoke script, PostgreSQL SQL verification with rollback fixtures and `EXPLAIN`, `npm ci`, `npm run typecheck`, `npm test`, `npx expo-doctor`, `npm run start -- --web --clear --port 8093`, `git diff --check` | Clean tracked-file checkout verification passed after fixing missing `api/composer.lock`, missing `api/tests/Unit/.gitkeep`, API-only unauthenticated JSON handling, and stale README/TSD/testing/roadmap evidence. Laravel tests passed 43 tests with 1 skipped and 151 assertions; Python tests passed 9; mobile tests passed 30; Expo Doctor passed 18/18; SQL D1-D4 executed with rollback fixtures. | Deployment, physical-device acceptance, private GitHub publishing, final submission messaging, and explanation video were not performed. Mobile npm install reported 11 moderate dependency vulnerabilities; no audit fix was applied. |

## Append-Only Template

Add future entries below without rewriting verified history.

| Date/time | Tool | Purpose | Prompt summary | Files affected | Commands run | Human review | Outcome | Limitations or corrections |
|---|---|---|---|---|---|---|---|---|
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD |
